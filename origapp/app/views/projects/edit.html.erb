<h1>Projects controller, edit action</h1>

<p>Edit this file in <tt>app/views/projects/edit.html.erb</tt></p>

<%= error_messages_for :project %>

<% form_for(@project, :action => url(:project, @project)) do %>
  <p>
    <%= submit_button "Update" %>
  </p>
<% end %>

<%= link_to 'Show', url(:project, @project) %> |
<%= link_to 'Back', url(:projects) %>
